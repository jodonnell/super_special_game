class BuzzSaw extends Sprite {
  constructor(x, y) {
    super();
    this.x = x;
    this.y = y;
    this.frame = 0;
    this.sprite = images.img.buzzsaw;
    this.currentFrame = this.sprite;
  }

  update() {}
}
